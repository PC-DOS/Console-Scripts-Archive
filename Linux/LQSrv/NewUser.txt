cd /media/dataX
sudo mkdir ./newuser

sudo useradd -d /media/dataX/newuser -m -s /bin/bash newuser
sudo usermod -aG users newuser
sudo passwd newuser

sudo chown -R newuser:newuser ./newuser
sudo chmod -R 750 ./newuser

# Sudo things
# Allow
sudo usermod -aG sudo newuser
# Disallow
sudo usermod -rG sudo newuser